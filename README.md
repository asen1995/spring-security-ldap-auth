The application starts and embedded ldap server on port 8389.

When user tries to authenticate - it will be authenticated against the embedded ldap server.
The data is loaded from the ldap-data.ldif file.
